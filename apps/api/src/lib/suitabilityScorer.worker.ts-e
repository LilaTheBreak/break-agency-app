/**
 * @file Worker handler for suitability scoring.
 * This placeholder ensures the worker can process jobs without crashing.
 */
export async function handler(job: any) {
  console.log(`[Worker: suitabilityScorer] Processing job ${job.id}`, job.data);
  return true;
}