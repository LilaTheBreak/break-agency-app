/**
 * @file Worker handler for building campaigns.
 * This placeholder ensures the worker can process jobs without crashing.
 */
export async function handler(job: any) {
  console.log(`[Worker: campaignBuilder] Processing job ${job.id}`, job.data);
  return true;
}