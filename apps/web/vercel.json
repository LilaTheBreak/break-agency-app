{
  "framework": "vite",
  "installCommand": "pnpm -w --filter @home/shared --filter @home/web install --frozen-lockfile",
  "buildCommand": "pnpm -w --filter @home/web build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
