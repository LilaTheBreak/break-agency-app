{
  "framework": "vite",
  "installCommand": "pnpm --dir .. -w install --frozen-lockfile",
  "buildCommand": "pnpm --dir .. --filter @home/web build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
