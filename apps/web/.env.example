VITE_API_URL=
VITE_GOOGLE_CLIENT_ID=

# DEV SAFE MODE - Bypasses auth for local development (DEV ONLY - never runs in production)
# Set to "true" to enable mock user and prevent white screens during local dev
# VITE_DEV_SAFE_MODE=true
